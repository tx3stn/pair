# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
project_name: pair
builds:
  - env: [CGO_ENABLED=0]
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X "github.com/tx3stn/pair/cmd.Version={{.Version}}"
